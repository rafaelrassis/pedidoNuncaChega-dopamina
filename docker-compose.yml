services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: pnc
      POSTGRES_USER: pnc
      POSTGRES_PASSWORD: pnc
    ports:
      - "5432:5432"
    volumes:
      - pnc_data:/var/lib/postgresql/data

volumes:
  pnc_data:
